<li>
	{{burger}}

	{{#if devoured}}
	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">Eaten</button>{{/if}}

	<button class="delete-burger" data-id="{{this.id}}">Delete</button>
</li>